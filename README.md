# Docker Sample App

A simple Flask application demonstrating Docker usage.

## Build and Run

```bash
docker build -t docker-sample-app .
docker run -p 5000:5000 docker-sample-app
```

Then visit http://localhost:5000
